{% extends ajaxMode == 'false' ? 'main.html.twig' : 'empty.html.twig' %}
{%  block main %}
    <h2>Nouveau commentaire</h2>
    
    {% if (error) %}
        <div class="alert alert-warning">{{error}}</div>
        <div class="alert alert-warning">{{error}}</div>
    {% endif %}

        <div class="alert alert-primary">
        <div class="alert alert-primary">
            <input type="hidden" id="postId" value="{{postId}}">
            
            {% if (loged == false) %}
            <div class="">
                <label for="name">Nom, prénom</label>
                <input class="form-control" type="text" id="name" placeholder="Nom" value="{{name}}">
            </div>
            <div class="">
                <label for="mail">E-mail</label>
                <input class="form-control" type="mail" id="mail" placeholder="E-mail" value="{{mail}}">
            </div>
            {% else %}
            <input type="hidden" id="name" value="{{name}}">
            <input type="hidden" id="mail" value="{{mail}}">
            {% endif %}

            <div class="area">
                <label for="comment">Message</label>
                <textarea class="form-control" id="comment" placeholder="Commentaire">{{comment}}</textarea>
            </div>
            <a class="btn btn-success" onclick="bj(this)" data-bj="newComment|postComment||postId,name,mail,comment">
                Envoyer</a>

            <div id="commentTest"></div>
        </div>
{% endblock %}
